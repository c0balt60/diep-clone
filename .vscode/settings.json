{
    "python.analysis.extraPaths": [
        "server"
    ],
    "python.envFile": "${workspaceFolder}/server/.env",
    "python.linting.pylintArgs": [
        "--init-hook",
        "import sys, os; sys.path.insert(0, os.path.join(os.getcwd(), 'server'))"
    ]
}
